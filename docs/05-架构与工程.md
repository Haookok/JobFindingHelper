# 架构与工程 — 面试知识手册

覆盖系统设计、云原生、安全三大板块，共 7 个知识点。

## 目录

1. [分布式一致性](#分布式一致性)
2. [缓存架构设计](#缓存架构设计)
3. [消息队列](#消息队列)
4. [Docker 核心原理](#docker-核心原理)
5. [Kubernetes 架构](#kubernetes-架构)
6. [Web 安全基础](#web-安全基础)
7. [认证授权与加密](#认证授权与加密)

---

# 分布式一致性

- **难度**: 深入
- **分类**: 系统设计 / 分布式
- **标签**: [CAP, BASE, Paxos, Raft, 2PC, 3PC, 最终一致性]

## 核心概念

分布式系统中多副本、网络分区与节点故障并存，**一致性**指多个副本对外呈现的数据状态是否满足约定语义。**强一致**代价高；工程上常在 **CAP/BASE** 框架下选择 **线性化**、**顺序一致**或**最终一致**等不同等级，并用 **共识算法** 或 **事务协议** 实现。

**副本与 quorum**：读写副本数的组合（如 **N、W、R**）决定**可调一致**：`W+R>N` 常可避免读两套旧值，但与**冲突合并**、**提示移交（hinted handoff）** 等机制需一起理解。

## 详细解析

**CAP 定理**：在**分区容错 P**（网络分区不可避免）前提下，**一致性 C** 与**可用性 A** 不能同时满格。实际解读应避免教条：分区是**时间片段内**的事件；系统可**分区时降级**（牺牲部分 A 或放松 C），恢复后再修复。

**BASE**：**Basically Available**（基本可用）、**Soft state**（软状态）、**Eventually consistent**（最终一致）。与 ACID 对照：强调**高可用 + 异步复制 + 可容忍短暂不一致**，通过消息补偿、对账等达到最终一致。

**Paxos**：解决**异步网络**下多节点对单一值（或日志）达成一致的**共识**问题。角色：Proposer、Acceptor、Learner；两阶段（Prepare/Promise 与 Accept）保证**安全性**（不会决议不同值）与**活性**（在多数派可达时可推进）。Multi-Paxos 将首轮 Prepare 优化为选主后只跑 Accept，近似**日志复制**。

**Raft**：更易教学的共识算法，强Leader；**选举**（任期、心跳）+ **日志复制**（匹配索引与任期）；通过**提交索引**定义已提交条目。与 Paxos **等价表达能力**，工程实现（etcd、部分 MQ/DB）更常见。

**两阶段提交（2PC）**：协调者 **Prepare**（参与者写日志、锁资源、投票）→ 全 Yes 则 **Commit**，任一 No 则 **Abort**。缺点：**同步阻塞**、协调者单点、**脑裂**时可能不一致；网络分区下参与者可能长期阻塞。

**三阶段提交（3PC）**：增加 **PreCommit**，减少部分阻塞窗口，但**无法根本解决**网络分区下的不一致问题，实现复杂，生产少用。

**最终一致性方案**：异步主从复制 + **版本向量/时间戳**冲突解决；**CQRS + 事件溯源**；**业务幂等与对账**；**Gossip** 传播；读时**多副本读修复（read repair）**。根据业务选择「读写何种副本」「是否同步副本数 W+R>N」等（类似 Dynamo 可调一致）。

**ZAB / Multi-Paxos 工程形态**：ZooKeeper 的 **ZAB** 与 Raft 同属**主从式日志复制**家族，强调**全序广播**；与「经典论文 Paxos」表述不同但目标一致：**已提交的提案不会被覆盖**。

**Saga 与 TCC**：长事务在微服务中常用 **Saga**（编排/协同 + 补偿）或 **TCC**（Try-Confirm-Cancel）替代全局 2PC，牺牲部分隔离性换**可用性与扩展性**。

**Outbox 模式**：业务写 DB 与发消息**同事务**写 outbox 表，异步进程读 outbox 投递 MQ，避免「DB 成功、消息丢失」或双写不一致，是**最终一致 + 可靠投递**的常见落地。

**线性化 vs 串行化**：**线性化**强调单寄存器式**实时顺序**；**可串行化**是事务隔离级别概念，二者相关但不等同，勿混为一谈。

**时钟问题**：**逻辑时钟（Lamport）**、**向量时钟**用于**因果**推断；**TrueTime** 用**时钟不确定性界**换全局时间序——面试提到「墙上时钟不可靠」即可建立好感。

**拜占庭容错**：Paxos/Raft 默认**崩溃容错**（非恶意）；**PBFT** 等才针对拜占庭节点，复杂度高，链与部分金融场景才会深入考。

**幂等键与去重**：在「至少一次投递」前提下，**业务层幂等**（订单号、去重表）往往是比强求分布式事务更经济的工程解。

**Lease 与 fencing token**：主从切换常用 **lease** 防**双主写入**；存储层用 **fencing** 拒绝过期主的写，可与共识选主组合使用。

## 示例代码

```text
# Raft 日志匹配（面试口述）
# AppendEntries 携带 prevLogIndex、prevLogTerm；
#  follower 不一致则拒绝，leader 递减 nextIndex 重试，直到对齐。
```

```python
# 2PC 语义的简化伪代码：展示协调者与参与者的两阶段
def two_pc(participants):
    if not all(p.prepare() for p in participants):
        for p in participants:
            p.abort()
        return False
    for p in participants:
        p.commit()
    return True
```

## 面试追问

- **追问 1**：**线性一致性**与**顺序一致性**、**因果一致性**的区别？ZooKeeper 提供的是哪一种？
- **追问 2**：Raft 中 **Leader 宕机** 与 **网络分区** 各会导致什么现象？**脑裂**如何避免（任期号）？
- **追问 3**：**Percolator** 或 **Spanner TrueTime** 如何在大规模系统里折中一致性与性能？
- **追问 4**：**Gossip** 协议的收敛性、防环与**反熵（anti-entropy）** 在 Cassandra 等系统中的角色？

## 常见误区

- 背 CAP 选「CA」系统——分区存在时**必须处理 P**，讨论常是**分区期间**选 C 还是 A。
- 认为 **3PC** 比 **2PC** 一定安全——无法证明在异步模型下解决所有不一致。
- 把 **Paxos == Raft** 画等号：思想相近但角色划分、选主与教学结构不同，优化路径也不同。
- 认为 **共识算法** 能解决一切：**成员变更**（如 Raft joint consensus）、**快照与日志压缩**、**时钟漂移** 仍是工程大坑。
- 把 **最终一致** 说成「不保证一致」——应表述为「在有限时间与无新写条件下收敛到相同状态」，并说明**业务可见窗口**。

**对比记忆**：2PC 偏**数据库跨库事务**；Paxos/Raft 偏**复制日志与元数据**；Saga/TCC 偏**长流程微服务**。

---

# 缓存架构设计

- **难度**: 进阶
- **分类**: 系统设计 / 缓存
- **标签**: [缓存穿透, 缓存击穿, 缓存雪崩, CacheAside, 热点Key, 一致性]

## 核心概念

缓存用于**降低延迟、减压数据源**。设计需同时考虑**正确性**（与 DB 一致）、**可用性**（故障与热点）与**成本**。典型问题：**穿透**（查不存在的数据）、**击穿**（热点键过期瞬间压垮 DB）、**雪崩**（大量键同时失效）。

**命中率与成本**：Key 设计要兼顾**粒度**（过细放大键空间与元数据，过粗放大失效粒度）；**预热**与**懒加载**策略取决于读模式与启动风暴。

## 详细解析

**缓存穿透**：请求的数据**根本不存在**，缓存永不命中，流量直达 DB。  
**方案**：对不存在 key 也缓存**短 TTL 空值**（注意布隆误判）；**布隆过滤器**前置拦截；API 层校验与限流。

**缓存击穿**：**单个热点 key** 过期瞬间，大量并发同时未命中 DB。  
**方案**：**互斥锁/单飞**（只允许一个线程回源）；**逻辑过期**（异步刷新）；**永不过期 + 后台更新**；热点 key **预热**。

**缓存雪崩**：**大量 key 同一时刻过期**或缓存集群宕机，请求洪峰打挂 DB。  
**方案**：TTL **加随机抖动**；**多级缓存**（本地 + 远程）；**熔断降级**；**主从/集群**高可用；必要时**限流排队**。

**缓存更新策略**：

- **Cache Aside（旁路）**：读先缓存后 DB 回写；写**先更 DB 再删缓存**（或先删再写需注意并发窗口）。实现简单，最常用。
- **Read Through**：应用只调缓存组件，未命中由组件**代查 DB 并回填**。
- **Write Behind（异步写）**：先写缓存，**异步批量刷盘**，吞吐高但**丢数据风险**与一致窗口大，需 WAL/队列保证。

**分布式缓存一致性**：无完美方案；常用 **TTL + 删除缓存**、**版本号/时间戳**、**Canal/Debezium 订阅 binlog** 失效缓存、**强一致**场景走 DB 或共识，缓存仅作加速。

**热点 Key**：单 key QPS 极高导致单分片/单节点瓶颈。  
**方案**：**本地缓存**多级；**拆分 key**（副本 key + 随机后缀读聚合）；**Redis 热 key 探测与迁移**；读写分离；必要时在网关做**聚合请求**。

**Write Through**：写操作**同时写缓存与 DB**（或由缓存组件代理），读路径简单，但写延迟与失败处理要设计好（双写失败顺序、重试）。

**内存与淘汰**：Redis 常用 **LRU/LFU/TTL** 等策略；要区分**缓存**与**持久化存储**职责，避免把缓存当唯一数据源却无回源能力。

**监控指标**：命中率、大 key、热 key、慢查询、连接数、内存碎片率；与 DB **QPS 突增**联动告警常能提前发现击穿/穿透。

## 示例代码

```python
import random

def cache_ttl_jitter(base_sec: int, spread: float = 0.2) -> int:
    """雪崩治理：在基础 TTL 上增加随机抖动。"""
    return int(base_sec * (1 + random.uniform(-spread, spread)))

# Cache Aside 删除缓存（写路径常见写法之一）
# def update_user(user):
#     db.save(user)
#     cache.delete(f"user:{user.id}")
```

```text
# 互斥回源（击穿治理思路，伪代码）
# if cache.miss(key):
#     with distributed_lock(f"lock:{key}"):
#         if cache.miss(key):
#             v = db.load(key)
#             cache.set(key, v, ttl)
```

## 面试追问

- **追问 1**：**先删缓存再写 DB** 与 **先写 DB 再删缓存** 各有哪些并发竞态？哪种更常被推荐及原因？
- **追问 2**：**Redis 集群**下热点 key 落在同一 slot 的优化手段？`HASH_TAG` 的利弊？
- **追问 3**：**多级缓存**一致性如何保证？本地缓存 TTL 与远程缓存如何配合？
- **追问 4**：**大 Key** 对集群迁移、持久化、网络抖动的影响？如何拆分与压缩？

## 常见误区

- 用**布隆过滤器**解决击穿——布隆主要防**穿透**；击穿要配合**互斥**或**逻辑过期**。
- **空值缓存** TTL 过长导致**长期脏读**真实已写入的数据——需**主动失效**或较短 TTL。
- 认为 **Cache Aside 删缓存** 能保证强一致——并发下仍有短暂不一致窗口，需业务容忍或版本控制。
- **互斥锁**实现不当导致**死锁**或**锁等待放大**——需**短超时**、**看门狗**或**单飞线程池**等配套。
- 把 **本地缓存（Caffeine/Guava）** 当成分布式一致源——多实例间**无协调**，只适合**可容忍短暂不一致**的读多写少场景。

**压测建议**：模拟热点键过期、批量 TTL 到期与缓存宕机，观察 DB **连接池**与 **p99** 是否突变。

---

# 消息队列

- **难度**: 进阶
- **分类**: 系统设计 / 消息中间件
- **标签**: [Kafka, RabbitMQ, RocketMQ, 消息丢失, 重复消费, 顺序消息, 积压]

## 核心概念

**消息队列（MQ）** 在上下游间插入**异步、解耦、削峰**的缓冲层：生产者投递消息，Broker 持久化与分发，消费者异步处理。代价是引入**延迟**、**运维复杂度**与**一致性语义**（至多一次、至少一次、精确一次需额外设计）。

**选型维度**：吞吐与延迟、顺序与事务需求、路由灵活性、运维与云厂商生态、团队熟悉度；没有「绝对最好」，只有**场景匹配**。

## 详细解析

**为什么要用 MQ**：

- **解耦**：上下游不直接依赖彼此可用性与接口形态。
- **异步**：缩短关键路径响应时间（下单后发消息，异步发邮件）。
- **削峰填谷**：突发流量先入队，消费者按能力拉取。
- **最终一致**：用事件驱动协调多系统（需注意**幂等与补偿**）。

**Kafka / RabbitMQ / RocketMQ 对比（面试口径）**：

| 维度 | Kafka | RabbitMQ | RocketMQ |
|------|-------|----------|----------|
| 模型 | **Topic-Partition** 日志流 | **Exchange-RoutingKey-Queue**，AMQP | Topic + **Queue**，借鉴 Kafka |
| 吞吐与延迟 | 高吞吐、偏**日志与大数据** | 灵活路由、低延迟场景多 | 阿里系场景，事务消息等 |
| 消费 | Consumer Group 分区**独占消费** | 多种模式（work queue、fanout 等） | 集群/广播消费 |
| 顺序 | **分区内**有序 | 单队列可有序 | **队列级**有序、顺序消息特性 |

**消息丢失**：

- **生产端**：同步/异步发送确认（Kafka `acks`、RabbitMQ **publisher confirm**）。
- **Broker**：**持久化**（磁盘、副本同步策略）；集群副本 ISR。
- **消费端**：**先处理业务再提交 offset**（可能重复）或先提交再处理（可能丢）；事务消息与**幂等**配合。

**重复消费**：网络重试、rebalance 导致**至少一次**常见。  
**方案**：**业务幂等**（唯一键、去重表、Redis setnx）、**状态机**、Kafka **幂等生产者**与**事务**（有代价）。

**顺序消费**：全局顺序成本高；通常**同一业务键**映射到**固定分区/队列**；消费者**单线程**或**内存锁**保证分区/队列内顺序；注意**故障切换**时顺序与积压权衡。

**消息积压处理**：**扩容消费者**（Kafka 加消费者不超分区数则需**加分区**）；**降级逻辑**（批量写、跳过非核心）；**临时转发**到备用 Topic 用更多实例消费；根因可能是**下游慢**或**消费 bug**，需监控**lag**。

**消费语义小结**：**at-most-once**（先提交再处理，可能丢）；**at-least-once**（先处理再提交，可能重复）；**exactly-once** 在分布式端到端往往需 **幂等 + 去重 + 事务/outbox**，Kafka 事务是**系统内**语义，不等价业务全局 EOS。

**延迟队列与死信**：**TTL + 延迟插件/定时扫描** 实现延迟任务；消费失败重试超限进 **DLQ**，避免毒消息阻塞主队列；需**告警与人工作业**闭环。

**Rebalance 与稳定性**：消费者组重平衡会导致**重复消费**与**短暂停顿**；应控制 **session.timeout**、**max.poll.interval**，避免长耗时处理阻塞心跳；**静态成员**（如 Kafka cooperative sticky）可减少不必要的重分配。

## 示例代码

```java
// Kafka 消费者：手动提交 offset，处理成功后再 commit（至少一次 + 业务幂等）
// consumer.subscribe(Collections.singletonList("orders"));
// while (true) {
//   ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(500));
//   for (ConsumerRecord<String, String> r : records) {
//     processIdempotent(r);  // 业务幂等
//   }
//   consumer.commitSync();
// }
```

```text
# RabbitMQ：work queue 公平分发（面试常提 prefetch）
# channel.basicQos(1);  // 未 ack 前不再投递新消息，防止慢消费者堆积不均
```

## 面试追问

- **追问 1**：Kafka **ISR**、**HW**、**LEO** 与「已提交消息」对消费者可见性的关系？
- **追问 2**：RabbitMQ **镜像队列**、**惰性队列** 分别解决什么问题？与 Kafka 副本有何不同？
- **追问 3**：**RocketMQ 事务消息** 的半消息与回查机制如何保证本地事务与发送一致？
- **追问 4**：**Kafka compacted topic** 与日志保留策略分别适用于什么数据形态？与 RabbitMQ 队列删除语义差异？

## 常见误区

- 追求**全局精确一次**却不谈**端到端**：跨 DB 与 MQ 需**事务消息/outbox** 等模式。
- **先 commit offset 再处理** 导致丢消息却被说成「至少一次」——语义要说清。
- 认为**加分区**能无限扩容消费者：**分区数**与**键分布**决定上限与热点。
- **消息体过大**导致 Broker/网络压力——应传 **引用**（OSS ID）而非巨 JSON，或与对象存储联动。

---

# Docker 核心原理

- **难度**: 进阶
- **分类**: 云原生 / 容器
- **标签**: [Docker, Namespace, Cgroups, UnionFS, 镜像, Dockerfile, 网络, 安全]

## 核心概念

**容器**不是虚拟机：它与宿主机共享内核，通过 **Linux Namespace** 隔离进程视图（PID、NET、MNT、USER 等），通过 **Cgroups** 限制与计量 CPU、内存、IO 等资源。**UnionFS（联合文件系统）** 与 **分层镜像** 叠加只读层与可写容器层，实现镜像复用与快速启动。

**Docker 引擎**（containerd/runc 等链路）负责镜像拉取、OCI 运行时创建容器、网络与卷挂载。**Dockerfile** 描述构建步骤；**多阶段构建**用前一阶段编译产物复制到精简运行镜像，减小攻击面与体积。

## 详细解析

**Namespace**：`pid` 隔离进程 ID；`net` 独立网络栈（接口、路由、iptables）；`mnt` 挂载点视图；`ipc`、`uts`、`user` 等分别隔离 IPC、主机名、UID/GID 映射（rootless 依赖 user namespace）。

**Cgroups v1/v2**：限制 CPU share/quota、内存上限、blkio、pids 数量等；OOM 时容器内进程可能被杀。理解 **limits vs requests**（K8s 语境）前先掌握 Docker 的 `--cpus`、`--memory`。

**镜像分层**：每条 Dockerfile 指令常产生一层；层缓存要求 **顺序稳定** 与 **把易变层置后**。`COPY`/`ADD` 前尽量少改以命中缓存。**镜像 digest** 唯一标识内容寻址层。

**Dockerfile 实践**：选 **官方 slim/alpine** 需权衡 libc 兼容与调试工具；合并 `RUN` 减少层数但可读性下降需平衡；**非 root 用户** `USER` 运行；`HEALTHCHECK` 声明健康探测；敏感信息用 **build secret / 运行时挂载**，勿 `ARG` 泄露进历史层。

**多阶段构建**：构建阶段装编译器与依赖，最终阶段只拷贝二进制与静态资源，避免把源码与密钥留在镜像。

**网络模式**：**bridge** 默认 NAT，容器有独立 IP；**host** 共享宿主机网络栈，性能高但端口冲突；**none** 无网卡；**overlay**（Swarm/K8s CNI）跨主机虚拟二层。**自定义网络**提供 DNS 容器名解析。

**数据卷**：**bind mount** 绑定宿主机路径，适合开发；**named volume** 由 Docker 管理，适合持久化数据。**只读挂载** `ro` 降低篡改风险。

**安全加固**：最小镜像、只读根文件系统、capability 降级（`--cap-drop`）、seccomp/AppArmor/SELinux、镜像扫描（CVE）、不在镜像内放 `.env` 密钥、定期更新基础镜像。

**OCI 与运行时**：**OCI 镜像规范** 与 **runc** 等运行时创建隔离进程；**containerd** 作为守护进程管理镜像与容器生命周期，Docker CLI 与 K8s 在多数发行版上最终都落到类似链路。面试可说清 **「镜像 = 只读层 + 配置元数据」**。

**`.dockerignore`**：排除 `.git`、`node_modules`、本地构建产物，减小 build context 上传时间并避免误拷机密。**构建缓存** 在 CI 中可配合 **BuildKit cache mount**（`--mount=type=cache`）加速依赖安装。

**日志与进程**：容器内 **PID 1** 需正确处理信号（如使用 `tini`/`dumb-init` 或 Go 程序自身转发 SIGTERM）；日志应写 **stdout/stderr** 由运行时收集，避免写满容器可写层。

**资源与 ulimit**：除 Cgroups 外，注意 **nofile** 等 ulimit；高并发服务在 compose/K8s 中需一并调优，否则「本地能跑、容器里 accept 失败」。

**镜像签名与供应链**：**Docker Content Trust**、**cosign** 等对镜像验签；拉取策略用 digest 固定版本，避免 `:latest` 漂移导致不可复现与投毒风险。

## 示例代码

```dockerfile
# syntax=docker/dockerfile:1
FROM golang:1.22-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /app -trimpath -ldflags="-s -w" ./cmd/server

FROM gcr.io/distroless/static-debian12:nonroot
COPY --from=build /app /app
USER nonroot:nonroot
ENTRYPOINT ["/app"]
```

说明：多阶段 + distroless 减小体积与 shell 攻击面；`nonroot` 降低权限。

**Compose 与 Swarm（了解）**：单机编排用 Compose；Swarm 提供简易集群与 overlay。与 K8s 相比，Swarm 组件更少但生态与扩展性较弱，面试可对比 **控制平面复杂度与 CNI 生态**。

**排查思路**：`docker inspect` 看挂载与网络；`docker logs` 看应用输出；`docker stats` 看 Cgroups 限额是否触顶；进入容器用 `docker exec` 时注意 **生产环境勿依赖交互 shell 排障常态化**。

## 面试追问

- **追问 1**：容器与 KVM/VM 在隔离强度、性能、内核共享上的根本差异？
- **追问 2**：写时复制（CoW）在镜像层与容器可写层如何工作？删除文件为何可能产生 whiteout？
- **追问 3**：bridge 网络下容器访问外网的 NAT 路径？host 模式对端口绑定与安全的影响？
- **追问 4**：为什么 Dockerfile 里 `RUN apt-get update && install` 常写在一行？缓存失效链路是什么？
- **追问 5**：rootless Docker 依赖哪些内核能力？与特权容器 `--privileged` 风险对比？

## 常见误区

- 把容器当 **轻量 VM** 忽视 **内核共享**：宿主机内核漏洞影响所有容器。
- **镜像层缓存** 被 `COPY . .` 提前打散，导致每次构建全量重装依赖。
- **Alpine + glibc 混用** 引发奇怪段错误；动态链接需一致 libc。
- **容器内以 root 跑服务** 与宿主机 **目录 777 bind mount** 扩大入侵后果。
- 认为 **多阶段构建** 自动安全——若 `COPY --from=build` 误拷入密钥或 `.git`，仍会泄漏。
- **PID 1 是 shell 脚本** 且未 `exec` 主进程——信号达不到应用，优雅退出失效。
- **`docker run --privileged` 等同大幅削弱隔离**——除非确有必要，生产应禁止或白名单 cap。
- **卷权限与 UID 映射** 混乱——bind mount 宿主机目录与容器 `USER` UID 不一致导致读写失败或权限过大。

---

# Kubernetes 架构

- **难度**: 进阶
- **分类**: 云原生 / Kubernetes
- **标签**: [K8s, API Server, etcd, Scheduler, kubelet, Service, 工作负载, HPA, RBAC]

## 核心概念

Kubernetes 是 **控制平面 + 数据平面（节点）** 的分布式系统：**API Server** 为统一入口，校验与持久化期望状态到 **etcd**；**Controller Manager** 内各类控制器通过 watch/informer **调谐（reconcile）** 实际状态；**Scheduler** 为 Pending Pod 绑定节点。**Node** 上 **kubelet** 驱动容器运行时创建 Pod，**kube-proxy**（或 eBPF 等实现）维护 Service 到端点的转发规则。

**Pod** 是最小调度单元，共享网络与存储卷；**生命周期**含 Pending、Running、Succeeded/Failed 等。**Service** 提供稳定虚拟 IP/DNS 与负载均衡到 Pod。**Deployment** 管理无状态副本滚动升级；**StatefulSet** 有序与稳定网络标识；**DaemonSet** 每节点（或子集）一份 Pod。

## 详细解析

**API Server**：认证（证书、Bearer Token、OIDC）、授权（RBAC）、准入（Mutating/Validating Webhook）；所有组件仅通过其访问集群状态，避免直连 etcd（除 etcd 运维）。

**etcd**：一致性与高可用 KV；存集群元数据与资源对象。备份与恢复策略是运维面试常考点。

**Scheduler**：过滤（资源、亲和性、污点容忍）与打分（资源均衡、亲和）；可扩展 **调度框架**。**kubelet**：拉取镜像、探针（liveness/readiness/startup）、挂载卷、上报节点与 Pod 状态。

**Pod 生命周期**：创建 → 调度 → 拉镜像 → init 容器 → 主容器；**重启策略** `Always`/`OnFailure`/`Never`。**终止**：先发 SIGTERM，宽限期后 SIGKILL；需应用优雅关闭连接。

**Service 类型**：**ClusterIP** 集群内；**NodePort** 节点端口暴露；**LoadBalancer** 云 LB；**ExternalName** CNAME。**Headless Service**（`clusterIP: None`）配合 StatefulSet 做 DNS 逐 Pod 解析。

**Deployment**：ReplicaSet 管理版本；滚动参数 `maxSurge`/`maxUnavailable`。**StatefulSet**：稳定 hostname、有序扩缩、常与 PVC 模板配合。**DaemonSet**：日志、监控、CNI 等节点级代理。

**HPA**：依据 CPU/内存或自定义指标（Metrics API / Prometheus Adapter）扩缩 Deployment/StatefulSet 等；需 metrics-server 或自定义 metrics 管道；存在 **冷却/抖动** 与 **延迟** 需注意。

**RBAC**：`Role`/`ClusterRole` 绑定 `RoleBinding`/`ClusterRoleBinding` 到 User/Group/ServiceAccount；**最小权限**为安全基线；与 **ABAC/Node** 授权模式对比时强调 RBAC 为主流。

**CNI 与网络插件**：Calico/Cilium/Flannel 等实现 Pod 网段、路由与策略网络；**NetworkPolicy** 在 L3/L4 限制东西向流量，默认放行需显式加固。**Service 与 EndpointSlice** 将标签匹配的 Pod 端点暴露给 kube-proxy 或数据面。

**CRD 与 Operator**：**CustomResourceDefinition** 扩展 API；**Operator** 模式用控制器管理有状态应用生命周期（备份、升级）。**准入 Webhook** 可强制安全策略（镜像仓库、资源配额、标签规范）。

**存储**：**CSI** 插件对接云盘/NFS；**PV/PVC** 抽象存储供给。**ConfigMap/Secret** 挂载为卷或环境变量，Secret 仅 base64 编码非加密，需 etcd 加密与 RBAC 保护。

**可观测性**：**cAdvisor** 指标经 kubelet 暴露；控制平面组件日志与审计（Audit Policy）用于合规与入侵分析。

## 示例代码

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: demo
  template:
    metadata:
      labels:
        app: demo
    spec:
      serviceAccountName: demo-sa
      containers:
        - name: app
          image: demo:1.0.0
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              memory: 256Mi
```

说明：`requests` 供调度与 HPA 参考，`limits` 防资源失控；生产应配 probe 与 PDB。

## 面试追问

- **追问 1**：Informer/Lister 相对直接 watch API Server 的优势？如何避免 relist 风暴？
- **追问 2**：kube-proxy iptables 与 ipvs 模式差异？Service 会话亲和性如何实现？
- **追问 3**：Pod 一直 Pending 时如何排查？哪些事件与调度失败相关？
- **追问 4**：Deployment 滚动更新失败如何回滚？`maxUnavailable` 为 0 时语义？
- **追问 5**：HPA 与 VPA、Cluster Autoscaler 分工？自定义指标链路有哪些组件？

## 常见误区

- 认为 **Pod IP 稳定**——重建即变；持久访问依赖 Service 或 StatefulSet DNS。
- **readiness 与 liveness 配反**——未就绪流量切入或死循环重启加剧故障。
- **给所有容器超大 limits** 导致 **节点超卖与噪声邻居**，调度看似成功运行 OOM。
- **ClusterRoleBinding 过宽**（如 cluster-admin 给默认 SA）造成横向移动风险。
- 忽略 **etcd 备份** 与 **API Server 证书轮换**，灾难恢复演练缺失。
- 把 **Secret 当加密存储**——默认可被有权限者解码；应配合 **EncryptionConfiguration** 与密钥管理。
- **NetworkPolicy 未部署却以为默认拒绝**——多数插件默认全通，需显式编写策略。
- **自定义 CRD 无版本迁移与校验**——集群升级时出现不可调和字段，引发控制器崩溃。

---

# Web 安全基础

- **难度**: 进阶
- **分类**: 安全 / Web
- **标签**: [XSS, CSRF, SQL注入, SSRF, 文件上传, CSP, CORS, OWASP]

## 核心概念

Web 安全围绕 **输入不可信**、**最小权限** 与 **纵深防御**。**XSS** 将恶意脚本注入页面被浏览器执行，分反射型（一次性 URL/输入回显）、存储型（入库后多人受害）、DOM 型（纯前端拼接 DOM 导致）。**CSRF** 利用已登录用户浏览器**自动带 Cookie** 发起非本意请求。

**SQL 注入** 将数据与命令边界破坏，执行任意 SQL。**SSRF** 诱导服务端请求内网或元数据服务。**文件上传** 若类型与路径失控可导致 WebShell 或路径穿越。**CSP** 限制脚本与资源来源；**同源策略**隔离不同源的 DOM/数据，**CORS** 是有条件的跨源读放宽。**OWASP Top 10** 汇总常见风险类别（版本会更新，面试答「注入、失效访问控制、加密失败」等大类即可）。

## 详细解析

**XSS 防御**：默认 **HTML 转义** 输出上下文敏感（属性/JS/CSS 规则不同）；`Content-Type` 与 charset 正确；`httpOnly` Cookie 降低窃 Cookie 后危害；**CSP** `default-src 'self'`、`script-src` Nonce/Hash 禁内联；框架自动转义与 **DOMPurify** 等清洗富文本。**模板与 `v-html`/innerHTML** 是高风险点。

**CSRF 防御**：**SameSite Cookie**（Lax/Strict）、**CSRF Token**（表单/Header 双提交）、关键操作 **二次验证**（密码、OTP）；校验 **Origin/Referer**（可缺失需兜底）。**JWT 放 localStorage** 不受同源 Cookie 自动发送保护，CSRF 形态变化但需防 XSS 窃 token。

**SQL 注入**：**参数化查询/预编译** 为根本；ORM 仍可能 raw SQL 注入。**最小权限** DB 账号、禁多语句（视引擎）与危险存储过程暴露。

**SSRF**：对内网 IP、元数据 URL（如 `169.254.169.254`）做 **白名单** 与 **禁用协议**（file、gopher）；解析后 **二次校验** IP 段；异步任务与 PDF 渲染等「用户可控 URL」是高频入口。

**文件上传**：白名单扩展与 **魔数** 校验、重命名、隔离存储（对象存储 + 无执行权限）、图片 **二次处理**、限制大小与扫描病毒；禁止上传路径用户可控拼接。

**CSP 与 CORS**：CSP 防数据外泄与脚本执行；CORS `Access-Control-Allow-Origin: *` 配 `credentials` 无效且易误配。**CORS 不是安全机制**——不能替代鉴权，仅放宽浏览器读响应。

**OWASP Top 10（典型）**：Broken Access Control、Cryptographic Failures、Injection、Insecure Design、Security Misconfiguration、Vulnerable Components、Authentication Failures、Software and Data Integrity、Logging Failures、SSRF（以官方当年版本为准）。

**失效访问控制与 IDOR**：**水平越权**（改 URL 或 ID 访问他人资源）、**垂直越权**（普通用户调管理接口）。应在服务端 **每次** 按会话身份做 **对象级授权**，勿仅依赖前端隐藏按钮。

**XXE**：XML 解析器允许外部实体可导致内网探测与文件读取；禁用外部实体、用 JSON 或安全解析配置。

**反序列化**：Java、Python pickle、部分 JSON 库的不安全反序列化可导致 RCE；使用 **类型白名单**、避免信任用户输入的类名。

**点击劫持**：`X-Frame-Options` 或 CSP `frame-ancestors` 禁止被恶意站点 iframe 嵌套诱导点击。

**开放重定向与钓鱼**：登录 `redirect` 参数未校验可导致钓鱼；应 **白名单** 目标 URL。

**安全响应头**：除 CSP 外，`X-Content-Type-Options: nosniff`、`Referrer-Policy`、`Permissions-Policy` 等降低浏览器侧攻击面。

**日志与监控**：登录失败、管理操作、SSRF 可疑目标应 **审计**；注意日志中 **勿记录密码与完整卡号**，并防日志注入（换行符污染格式）。

**速率限制与账户枚举**：登录、短信、优惠券接口需 **限流与锁定策略**，防止撞库与资源耗尽；错误提示勿区分「用户不存在」与「密码错误」以免 **用户枚举**。

**依赖与供应链**：第三方 JS、npm 包、CI 插件均可能成为攻击面；使用 **SRI**（Subresource Integrity）固定 CDN 脚本哈希，并定期 **依赖扫描**。

**GraphQL 特有风险**：**深度/复杂度限制** 防 DoS；**字段级授权** 防越权 introspection 泄露内部模式；批量查询需 **成本分析**。

**WebSocket 与 SSE**：鉴权需在 **握手** 校验 Token/Cookie，勿仅靠首包后无状态；防 **跨站 WebSocket 劫持** 与消息注入。

**子域接管**：废弃 CNAME 指向可被注册的第三方服务时，攻击者可托管恶意内容于「可信子域」；应清理 DNS 与证书。

## 示例代码

```http
Content-Security-Policy: default-src 'self'; script-src 'self'; object-src 'none'; base-uri 'self'
Set-Cookie: sid=...; HttpOnly; Secure; SameSite=Lax; Path=/
```

说明：CSP 需按站点实际脚本拆分策略；Cookie 同时 **Secure**（HTTPS）与 **HttpOnly**。

## 面试追问

- **追问 1**：存储型 XSS 与反射型在利用链与日志留存上的差异？CSP Nonce 如何与 SSR 配合？
- **追问 2**：双重 Cookie 提交与 Synchronizer Token 模式各适用什么架构？
- **追问 3**：预编译为何能防 SQL 注入？ORM 的 `order by` 动态拼接如何安全实现？
- **追问 4**：SSRF 绕过常见手法（DNS 重绑定、302、IPv6、内网别名）如何防护？
- **追问 5**：`Access-Control-Allow-Origin` 反射任意 Origin 的风险？与 JSONP 对比？
- **追问 6**：IDOR 与「业务逻辑漏洞」在测试用例上如何覆盖？API 网关能否单独解决？

## 常见误区

- 仅前端校验输入即认为安全——**服务端必须校验**。
- 认为 **CORS 关闭** 就能防攻击——攻击者可用 **非浏览器客户端** 直接请求。
- **黑名单过滤 XSS**（关键字替换）易被编码与变形绕过；应 **上下文编码 + CSP**。
- **JWT 长期无刷新** 与 **密钥放前端**；或把 CSRF 防护简单等同于「用了 JWT」。
- 文件上传只校验 **扩展名** 忽视 **内容与存储路径**，导致解析器漏洞与 RCE。
- **仅依赖前端路由隐藏管理页**——直接请求 API 仍可越权；授权必须在服务端。
- **CSP 配 `unsafe-inline` 与 `unsafe-eval`** 基本抵消 XSS 缓解；应逐步收紧或采用 Nonce。
- **把「同源」与「同站（Site）」混用**——SameSite Cookie 按 eTLD+1 划分，子域策略需单独设计。

---

# 认证授权与加密

- **难度**: 进阶
- **分类**: 安全 / 身份与密码学
- **标签**: [Session, JWT, OAuth2, TLS, 对称加密, 非对称加密, 哈希, RBAC, 零信任]

## 核心概念

**认证（Authentication）** 回答「你是谁」，**授权（Authorization）** 回答「你能做什么」。**Session**：服务端存会话状态，客户端常持 **session id**（Cookie）；易撤销、服务端有状态。**JWT**：自包含声明，服务端用密钥校验签名；无状态扩展好，**吊销与刷新**需额外设计（黑名单、短 TTL + refresh、版本号）。

**OAuth 2.0** 是 **授权框架**（委托访问），不是认证协议；OpenID Connect 在 OAuth2 上提供身份层。**HTTPS/TLS** 在传输层提供机密性、完整性与身份验证：**握手**交换密码套件，证书链校验服务端身份，协商对称会话密钥，记录层用 AEAD 等保护应用数据。

**对称加密**（AES-GCM 等）加解密同一密钥，快，适合大量数据；**非对称**（RSA、ECC）公钥加密/私钥签名，解决密钥分发，通常用于握手与签名。**哈希**（SHA-256）用于完整性；**密码存储**用 **bcrypt/Argon2/scrypt** 等慢哈希 + 盐，**禁止明文或可逆**。**MD5/SHA-1** 已不适合安全场景。

**RBAC** 按角色绑定权限；**ABAC** 按属性（用户、资源、环境）策略决策，灵活但复杂。**零信任** 默认不信任网络位置，持续验证身份与设备态势，最小权限与微分段。

## 详细解析

**Session vs JWT**：Session 适合强服务端控制与即时登出；JWT 适合跨服务与移动端，但要注意 **XSS 存 localStorage 窃 token**、**体积过大**、**算法 none/HS256 误用**（公钥当 HMAC secret）等坑。**Refresh Token** Rotation 可降低长期泄露风险。

**TLS 握手（1.2 简述）**：ClientHello（随机数、套件）→ ServerHello + 证书 + ServerKeyExchange → 客户端验链、生成预主密钥 → 双方派生会话密钥 → Finished 校验握手完整性。**TLS 1.3** 减少往返、仅安全套件、0-RTT 有重放权衡。

**对称 vs 非对称**：大数据用对称；非对称用于密钥封装或数字签名。**混合加密** 是常态。选用 **AEAD**（如 AES-GCM、ChaCha20-Poly1305）避免自行拼接 IV/MAC。

**哈希与密码**：SHA-256 用于文件完整性、证书指纹；**不应用 SHA 存密码**。bcrypt 工作因子可调对抗硬件加速；Argon2 获密码哈希竞赛推荐。** pepper**（服务端密钥）与 **per-user salt** 组合使用需注意密钥管理。

**RBAC/ABAC**：RBAC 模型清晰、审计简单；ABAC 适合细粒度动态策略（如「仅工作时间 + 部门匹配」）。Kubernetes、云 IAM 多为 RBAC + 资源级策略的变体。

**零信任实践**：身份（MFA、设备证书）、网络（微分段、服务网格 mTLS）、数据（加密与 DLP）、可观测与持续评估；与传统边界 VPN 对比强调 **身份为新的边界**。

**mTLS 与服务身份**：双向 TLS 在微服务间验证 **服务证书**（SPIFFE/SPIRE 等），结合 **短周期证书** 降低泄露影响。与「仅边缘 TLS」相比，内网横向移动难度提高。

**密钥管理**：**KMS/HSM** 保护主密钥；应用侧使用 **信封加密**（数据密钥本地加解密，KEK 在 KMS）。**轮换** 需兼容解密旧密文与多版本密钥 ID。

**JWE 与 JWS**：**JWS** 防篡改；敏感声明若必须自包含传输用 **JWE** 加密。**JWK** 轮换通过 `kid` 头选择验证密钥；防止 **alg 混淆**（RS256 token 被当作 HS256 用公钥验）。

**证书与 PKI**：链式校验至信任锚、检查 **hostname/SAN**、**OCSP/CRL** 吊销；**Let's Encrypt** 等自动化 ACME。客户端证书在企业 **B2B API** 中仍常见。

**合规与隐私**：PII 最小化、字段级加密、**KMS 审计日志**；跨境传输注意法规与 **DPA**。

**随机数与安全**：密钥、nonce、session id 须用 **CSPRNG**（如 `crypto/rand`），勿用时间戳或可预测种子。**Nonce 与 IV** 在 AEAD 中不可复用同一密钥下组合，否则机密性与完整性崩溃。

**数字签名与证书透明度**：业务层 **JWS/自定义 API 签名** 与 TLS 证书不同；重要公钥基础设施可结合 **CT log** 监控误签发。

**口令策略与 MFA**：长度优先于复杂轮换频率（NIST 倾向 **长口令 + 泄露检测**）；敏感操作用 **TOTP/WebAuthn** 等多因素，降低钓鱼与重放风险。

**API Key 管理**：Key 应 **可轮换、可吊销、最小权限**；勿提交到仓库；网关层做 **作用域与速率** 控制，与终端用户 JWT 分层设计。

**SAML/OIDC 联邦（简述）**：企业 SSO 常用 **SAML 断言** 或 **OIDC 授权码流**；注意 **断言签名、时钟偏移、重放**（`NotOnOrAfter`、一次性 `InResponseTo`）与 **元数据轮换**。

**侧信道（了解）**：比较密码或 MAC 时用 **常数时间** 算法，避免计时泄漏；高安全场景关注 **缓存侧信道** 与实现库选型。

## 示例代码

```text
# 密码存储（概念示意）：使用 bcrypt 等库，勿自行实现
# 验证：bcrypt.checkpw(plain, stored_hash)
# JWT：alg 固定允许列表，校验 iss/aud/exp，密钥轮换
```

说明：具体语言用官方推荐库（如 `golang.org/x/crypto/bcrypt`、`jose` 族）；避免在业务代码里手写加密流程。

## 面试追问

- **追问 1**：JWT 放在 Cookie（HttpOnly）与 Header 的 CSRF/XSS 权衡？如何设计吊销？
- **追问 2**：TLS 1.3 相比 1.2 减少了哪些消息？前向保密（PFS）依赖什么？
- **追问 3**：为何 RSA 加密大数据要混合 AES？OAEP 与 PKCS#1 v1.5 填充风险？
- **追问 4**：OAuth2 的 authorization code flow 为何比 implicit 更安全？PKCE 解决什么问题？
- **追问 5**：RBAC 角色爆炸问题如何用职责分离或 ABAC 缓解？策略引擎部署在哪一层？
- **追问 6**：mTLS 与 JWT 在服务间身份上的取舍？SPIFFE 如何统一工作负载身份？

## 常见误区

- 把 **OAuth2 当登录协议** 不明确 **resource owner** 与 **OpenID Connect** 边界。
- **JWT 不设 exp** 或 **密钥过弱**；或 **在 payload 存敏感明文**（仅 Base64 非加密）。
- 用 **MD5/SHA256 直接哈希密码**、**固定盐** 或 **短随机盐**。
- **TLS 终止在边缘后内网明文** 传输敏感数据；忽略 **证书链与 SNI** 配置。
- 零信任 **只买产品不改造身份与分段**，流于口号；或把 **VPN 下掉却无替代控制**。
- **自签名证书关闭校验**（`InsecureSkipVerify`）上线——中间人可读改流量；应使用可信 CA 与正确校验。
- **同一 RSA 密钥既加密又签名** 且无清晰协议边界——增加密码分析面；实践上多用专门协议与套件。
- **Refresh Token 无绑定**（设备/轮替检测）——被盗后可长期续期；应 **rotation + reuse 检测**。
