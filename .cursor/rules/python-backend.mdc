---
description: Python FastAPI 后端开发规范
globs: backend/**/*.py
alwaysApply: false
---

# Python 后端规范

## 路由组织
- 按功能拆分到 `routers/` 目录，每个文件一个 `APIRouter`
- 在 `main.py` 中统一注册，设置 prefix 和 tags
- 路由函数名用动词开头：`get_questions`、`create_interview`

## 数据校验
- 请求/响应一律用 Pydantic BaseModel
- Model 定义在 `models/` 目录
- 字段添加 `Field()` 校验和中文 description

## 异步优先
- 路由函数用 `async def`
- I/O 操作用 `await`（httpx、aiofiles）
- CPU 密集任务用 `run_in_executor`

## 错误处理
统一返回格式：
```python
{"code": 200, "message": "success", "data": ...}
{"code": 400, "message": "参数错误", "detail": "..."}
```
用 `HTTPException` 抛出，全局异常处理器捕获

## 类型标注
- 所有函数参数和返回值必须有类型标注
- 使用 `list[str]` 而非 `List[str]`（Python 3.10+）
