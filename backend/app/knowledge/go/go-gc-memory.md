# Go 内存管理与 GC

- **难度**: 深入
- **分类**: Go / 运行时 / 内存
- **标签**: [GC, 三色标记, 写屏障, STW, 逃逸分析, sync.Pool, 内存对齐]

## 核心概念

Go 采用 **并发三色标记清除** 为主的垃圾回收：对象从白（未访问）、灰（已发现子引用待扫描）到黑（已扫描且子引用均为黑或灰）。**写屏障**在并发标记阶段记录指针变更，保证「黑对象不直接指向白对象」等不变式，使标记与 mutator 并发进行时仍安全。

**STW（Stop-The-World）** 在部分阶段仍存在（如标记开始与结束的小窗口），Go 持续降低暂停时间与 **Pacing**（根据堆增长与 `GOGC` 目标触发周期）。**逃逸分析**决定变量分配在栈（随帧释放）还是堆（GC 管理）；逃逸到堆会增加 GC 压力。

**`sync.Pool`** 是对象复用池，GC 可能清空池中对象，适合 **短生命周期、可丢弃** 的临时缓冲（如 `[]byte`），不适合当作长期缓存。**内存对齐**影响结构体字段顺序与填充（padding），合理重排可减小 `sizeof`。

## 详细解析

**三色标记流程（简化）**：根集合（栈、全局、寄存器等）出发标记可达；并发阶段与用户代码并行；需写屏障修正并发写。**混合写屏障**等演进减少重扫与 STW。理解面试重点：**为何需要写屏障**、**浮动垃圾**（并发期误标为存活可接受）。

**`GOGC`**：默认 100 表示堆相对上次 live 增长 100% 左右触发下一轮；调大降低 GC 频率、增大内存占用与峰值延迟；调小更频繁回收。**生产调优**应结合 `GOMEMLIMIT`（Go 1.19+）、延迟与 RSS 监控，避免盲目设极端值。

**逃逸分析触发示例**：返回局部变量指针、闭包捕获指针、对接口赋值（部分情况）、切片元素为指针且逃逸、过大栈对象等。可用 **`go build -gcflags=-m`** 查看逃逸决策。

**栈 vs 堆**：栈分配廉价、局部性好；堆分配支持更长生命周期与逃逸语义。小对象还可能经 **mcache/mcentral** 等 **tcmalloc 风格** 分配器管理，减少锁竞争。

**`sync.Pool` 语义**：`Get` 可能返回 `nil` 或任意池化对象，调用方需初始化；`Put` 勿放带状态且未重置的对象以免数据串台；**两次 GC 之间** 对象可能被复用也可能被回收，不能当持久存储。

**内存对齐**：CPU 按自然对齐访问更高效；结构体字段按大小从大到小或按工具建议排列可减少 padding。原子操作常要求 **64 位对齐**（尤其在 32 位或混合布局中），错误对齐可能导致崩溃或未定义行为。

**GC 与 CPU**：标记、清扫、辅助标记（mutator assists）都会消耗 CPU；分配速率过高会触发更频繁 cycle 与 assist，表现为 **延迟毛刺**。压测应同时看 **alloc_rate、GC pause、RSS**。

**终结器（finalizer）与 unsafe**：`runtime.SetFinalizer` 执行时机不确定且可能延长对象寿命，关键资源应用 **显式 Close**；`unsafe.Pointer` 与 CGO 可能产生 **GC 不可见的指针**，需遵守 `unsafe` 规则或 `runtime.KeepAlive`。

**小对象与分配器**：小对象经 span 与 mcache 分配，减少碎片与锁；理解「为何 map/slice 增长会分配新底层」有助于减少无谓扩容。大对象走特殊路径，可能触发更明显 STW 片段（随版本变化，面试答「关注版本发行说明」）。

**编译器内联与逃逸**：内联可能改变逃逸结果；微基准时勿过度解读单次 `-m` 输出，以 **pprof heap、alloc_space** 为准。

## 示例代码

```go
package main

import "sync"

var bufPool = sync.Pool{
	New: func() interface{} {
		b := make([]byte, 0, 1024)
		return &b
	},
}

func borrow() *[]byte {
	p := bufPool.Get().(*[]byte)
	*p = (*p)[:0]
	return p
}

func release(p *[]byte) {
	bufPool.Put(p)
}
```

说明：`Pool` 存指针避免切片头拷贝歧义；`New` 返回空切片底层数组可被复用。高并发下仍需压测验证是否优于直接分配。

## 面试追问

- **追问 1**：并发标记时若没有写屏障会出现什么错误？混合写屏障大致解决什么问题？
- **追问 2**：`GOGC` 与 `GOMEMLIMIT` 如何共同影响吞吐与 OOM？什么场景适合调低 `GOGC`？
- **追问 3**：哪些代码模式最容易导致逃逸？`interface{}` 传值为何常导致堆分配？
- **追问 4**：`sync.Pool` 与 LRU 缓存本质区别？为何不能把 Pool 当连接池唯一手段？
- **追问 5**：Go 的 GC 是压缩式还是非压缩式？对内存碎片与 RSS 有何影响？

## 常见误区

- 认为 **调低 `GOGC` 一定降低延迟**——可能增加 CPU 开销与吞吐下降，需指标权衡。
- **滥用 `sync.Pool` 缓存业务对象**——GC 清空与 `Get` 不确定性会导致逻辑错误；长生命周期应显式池或缓存层。
- 忽略 **大堆 + 长指针链** 对标记阶段的影响；应减少不必要的指针与深层结构。
- 结构体字段 **按「美观」排序** 导致多余 padding；应用 `fieldalignment` 或手工优化热点路径。
- 认为 **栈上对象可以取地址返回**——逃逸后实际在堆；依赖「栈上一定快」而不看逃逸报告会误判性能。
- 依赖 **`SetFinalizer` 释放文件/锁**——可能很晚才跑或永不跑；应 `defer Close` 或显式生命周期管理。
- **`sync.Pool` 里 `Put` 大切片不切 cap**——其它 `Get` 方拿到巨大底层数组，长期占用内存；归还前应 `[:0]` 或限制 cap。
- 在 **32 位** 上对含 `sync/atomic` 64 位字段的结构体 **随意嵌入排序**——未对齐字段引发崩溃；将 64 位原子字段放在结构体顶部是常见惯例。
