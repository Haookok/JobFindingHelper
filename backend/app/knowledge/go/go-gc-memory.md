# Go 内存管理与 GC

- **难度**: 深入
- **分类**: Go / 运行时 / 内存
- **标签**: [GC, 三色标记, 写屏障, STW, 逃逸分析, sync.Pool, 内存对齐]

## 核心概念

Go 的垃圾回收就像**自动打扫卫生的保洁阿姨**。你不用自己 `free` 内存，保洁阿姨会定期来检查：哪些东西还有人在用（有引用），哪些已经没人要了（不可达），没人要的就收拾掉。

Go 的 GC 用的是**并发三色标记清除**——保洁阿姨边打扫边让你继续干活，尽量不让你停下来等。"三色"是标记过程的三种状态：白色（还没看过）、灰色（看了但还没看完它引用的东西）、黑色（看完了，确认还有人用）。最后剩下的白色就是垃圾。

**逃逸分析**则决定"东西放哪"——能放在工位上（栈）就放栈上，方法结束自动清空，根本不需要保洁阿姨管；必须共享的才放仓库（堆），由 GC 管理。

## 详细解析

### 三色标记——保洁阿姨怎么边打扫边让你干活？

如果阿姨必须让全公司停工才能打扫（STW），那太浪费了。并发标记的思路是：阿姨在标记垃圾的同时，员工还在创建和修改对象。但这有个问题——阿姨刚标完一个东西"没人用"，员工又引用了它怎么办？

**写屏障**就是解决这个问题的：员工每次修改引用时，顺手通知一下阿姨"我改了这里"，这样阿姨不会误删还在用的东西。Go 用的是**混合写屏障**，能在很短的 STW 窗口内完成标记的开始和结束，大部分时间阿姨和员工并行工作。

### GOGC 和 GOMEMLIMIT——调节保洁频率

**GOGC**（默认 100）：意思是"堆内存增长到上次 GC 后存活量的 2 倍（100%）时，触发下一轮 GC"。调大 = 阿姨来得少，省 CPU 但占内存多；调小 = 阿姨来得勤，内存省但 CPU 开销大。

**GOMEMLIMIT**（Go 1.19+）：给内存设个硬上限——"总共只能用这么多"，GC 会在接近上限时更积极地回收。生产环境建议结合两者使用。

### 逃逸分析——能不进仓库就别进

栈上分配几乎零成本（方法结束自动清空），堆上分配才需要 GC。以下情况变量会"逃逸"到堆上：
- 函数返回了局部变量的指针（调用方还要用，不能随函数结束清掉）
- 闭包捕获了外部变量的指针
- 赋值给 `interface{}` 类型（编译器不确定大小和生命周期）
- 对象太大，栈放不下

用 `go build -gcflags=-m` 可以查看逃逸分析的决策。

### sync.Pool——对象回收站

`sync.Pool` 就像办公室的"共享文具盒"——用完的笔放回去，下个人直接拿，不用每次都买新的。适合**高频创建且可重置**的临时对象（如 `[]byte` 缓冲区）。但注意：保洁阿姨每次来都可能清空文具盒（GC 时 Pool 中的对象可能被回收），所以**不能当作持久缓存**。

### 内存对齐——别浪费空间

结构体的字段在内存中要按一定规则对齐（CPU 读取效率要求）。字段排列顺序不当会产生"空洞"（padding）。把大字段放前面、小字段放后面，可以减少空洞，节省内存。

## 示例代码

```go
package main

import "sync"

// sync.Pool：共享的 byte 缓冲区回收站
var bufPool = sync.Pool{
	New: func() interface{} {
		b := make([]byte, 0, 1024)  // 预分配 1KB 容量
		return &b
	},
}

func borrow() *[]byte {
	p := bufPool.Get().(*[]byte)  // 从回收站拿一个
	*p = (*p)[:0]                  // 重置长度，但保留底层数组
	return p
}

func release(p *[]byte) {
	bufPool.Put(p)  // 用完放回去
}
```

## 面试追问

- **面试官可能会这样问你**：为什么需要写屏障？没有会怎样？（并发标记时，如果员工把一个"白色对象"挂到"黑色对象"下面，阿姨不知道，就会误删这个白色对象——程序崩溃）
- **面试官可能会这样问你**：GOGC 调低一定能降低延迟吗？（不一定！GC 频率增加意味着更多 CPU 开销用在 GC 上，可能影响吞吐量）
- **面试官可能会这样问你**：什么代码模式最容易导致逃逸？（返回指针、`interface{}` 传值、闭包捕获指针。减少逃逸 = 减少 GC 压力）
- **面试官可能会这样问你**：sync.Pool 和 LRU 缓存有什么区别？（Pool 里的对象 GC 时随时可能被清掉，不能当持久存储；LRU 是你自己管理生命周期的缓存）
- **面试官可能会这样问你**：Go 的 GC 是压缩式的吗？（不是，Go GC 不移动对象，所以可能有碎片问题。相比之下 Java 的 G1/ZGC 可以做压缩整理）

## 常见误区

- **很多人会搞混的地方**：以为 GOGC 调低就一定好——GC 更频繁意味着 CPU 花更多时间在回收上，吞吐量可能下降。
- **很多人会搞混的地方**：把 sync.Pool 当连接池用——GC 随时可能清空 Pool，数据库连接这种宝贵资源应该用专门的连接池管理。
- **很多人会搞混的地方**：以为"栈上分配一定比堆上快所以要避免所有逃逸"——不必过度优化，先用 pprof 确认热点，再有针对性地减少逃逸。
- **很多人会搞混的地方**：依赖 `runtime.SetFinalizer` 释放文件/锁——执行时机不确定，可能很晚才跑。应该用 `defer file.Close()` 显式管理。
