# JVM 内存模型与垃圾回收

- **难度**: 进阶
- **分类**: JVM / 内存 / GC
- **标签**: [堆, 栈, 方法区, GC Roots, CMS, G1, ZGC, STW]

## 核心概念

JVM 运行时数据区包括：**程序计数器**（线程私有，当前字节码行号）、**虚拟机栈**（栈帧：局部变量表、操作数栈等）、**本地方法栈**、**堆**（对象实例与数组，GC 主战场）、**方法区**（JDK8 起元空间 Metaspace 使用本地内存，存类元数据、常量池等）。直接内存（NIO）由操作系统管理，不受堆大小直接限制但受本机内存约束。

**可达性分析**从 **GC Roots** 出发标记存活对象，其余可回收。常见 Roots：栈上引用、静态变量、JNI 引用、同步锁持有对象等。**STW（Stop-The-World）** 指 GC 线程暂停应用线程的阶段，低延迟收集器致力于缩短或并发化标记/整理过程。

## 详细解析

**分代假设**：大部分对象朝生夕死；老年代对象存活久。Young GC 回收新生代（Eden + Survivor），晋升规则与年龄计数相关；**Full GC** 往往涉及老年代与方法区（或元空间），代价更大。

**CMS**：并发标记清除，追求低停顿，但会产生**碎片**，可能 concurrent mode failure 退化为 Serial Old；JDK9 起标记为 deprecated。

**G1**：将堆划为多个 Region，**增量回收**，可预测停顿时间目标；适合大堆，混合回收 Young + 部分 Old。

**ZGC / Shenandoah**：**染色指针**或读屏障等，目标亚毫秒级 STW（与堆大小弱相关），JDK 版本演进中可用性提高。

对比维度：**吞吐量 vs 延迟**、是否压缩、STW 长度、内存开销、JDK 支持与运维复杂度。

**Serial / Parallel（Throughput）**：新生代复制、老年代标记整理或标记压缩；追求吞吐量时 STW 可能较长，适合批处理或后台任务。

**OOM 常见类型**：`Java heap space`（堆满）、`Metaspace`（类元数据过多）、`Direct buffer memory`（直接内存）、`GC overhead limit exceeded`（GC 时间占比过高）。定位需结合 dump 与分配热点。

**安全点与安全区域**：STW 前线程需到达**安全点**（方法调用、循环出口等）；正在执行 JNI 或长时间运算可能通过**安全区域**避免破坏根集合一致性。

**引用类型**：强引用默认；**软引用**在内存紧张时可回收，适合缓存；**弱引用**下次 GC 即回收；**虚引用**用于跟踪对象回收时机。与可达性分析结合理解「何时真正释放」。

**Minor GC vs Major GC**：命名在不同收集器上略有差异，一般 Young GC 只扫新生代，Old GC 扫老年代；Full GC 常含整堆与方法区/元空间，日志中需对照具体收集器解读。

**吞吐与延迟权衡**：高吞吐收集器可能用更长 STW 换更少总 GC 时间；低延迟收集器用并发标记、增量回收换更短停顿，可能占用更多 CPU 或内存写屏障开销。

**ZGC 要点（面试版）**：并发整理、染色指针、Region 化；JDK17+ 生产可用度提升；与 G1 相比更强调**极低停顿**，但需关注版本与平台支持。

**排查思路**：先 GC 日志与指标（频率、停顿、晋升失败），再 heap dump 看大对象与泄漏路径，最后结合代码热点与缓存生命周期治理。

**Epsilon GC（了解）**：几乎不回收的「No-Op」收集器，用于短生命周期应用性能基线测试，生产不可用。

**TLAB（Thread Local Allocation Buffer）**：线程在 Eden 上优先从本地缓冲区分配，减少同步；与「ThreadLocal 变量」概念不同，勿混用术语。

**记忆集与写屏障**：分代 GC 需记录**老年代指向新生代**的指针，Young GC 时扫描记忆集避免全表扫描老年代；写屏障在引用写入时维护该集合。

## 示例代码

```java
// 演示栈上引用作为 GC Root：方法内局部引用指向的对象在方法返回前不可被回收
public class GcRootDemo {
    public static void main(String[] args) {
        byte[] big = new byte[1024 * 1024];
        System.gc(); // 仅建议，不保证立即 Full GC
        System.out.println(big.length);
    }
}
```

排查时可结合 `jmap`、`jstat`、GC 日志与 MAT 分析 Dominator Tree。

口述题可画简图：栈帧引用指向堆对象，静态区引用指向元数据与堆单例；Young GC 存活对象复制到 Survivor，年龄到阈值晋升老年代。

**JDK 演进一句话**：JDK8 永久代移除、字符串常量池与静态变量细节迁移，面试常顺带考「方法区到底在哪」。

**Native Memory Tracking（NMT）**：排查元空间、线程栈、直接内存等综合占用时可开启 JVM NMT 辅助判断「堆外」压力来源。

## 面试追问

- **追问 1**：三色标记法与浮动垃圾（floating garbage）是什么？CMS 的增量更新 vs G1 的 SATB 简述？
- **追问 2**：`finalize` / `Cleaner` 与可达性分析、两次标记的关系？为何不推荐依赖 finalize 释放资源？
- **追问 3**：栈帧里局部变量表槽位与对象引用：什么情况下对象还能被回收？跨代引用与 **Card Table / Remembered Set** 的作用？
- **追问 4**：`-Xms/-Xmx`、新生代比例、Survivor 区与晋升阈值调优的取舍？G1 的 `MaxGCPauseMillis` 能否「保证」停顿上限？

## 常见误区

- 把 **Java 内存模型（JMM，happens-before、可见性）** 与 **JVM 内存区域（堆栈方法区）** 混为一谈——前者是并发语义规范，后者是运行时布局。
- 认为调用 `System.gc()` 就会立刻、可控地 Full GC——只是**建议**，具体由 JVM 决定。
- 认为「Full GC 越少越好」而无限调大堆——延迟与停顿可能变差，且**元空间**泄漏同样会导致问题。
- 把 **栈内存溢出（StackOverflowError）** 与 **堆 OOM** 混用同一套调参手段——栈深度由线程栈大小 `-Xss` 与调用链决定，与堆扩容无关。
- 认为 **方法区** 在 JDK8 后「进了堆」——元空间在**本地内存**，受 `MaxMetaspaceSize` 等约束，与堆分离管理。
